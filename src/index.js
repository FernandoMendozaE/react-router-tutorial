import React from 'react'
import ReactDOM from 'react-dom' // ! ReactDom es una libreria que nos permite renderizar el componente en el DOM

import App from './App'

ReactDOM.render(<App />, document.getElementById('root'))
